import testModule from './modules/module';

testModule('Hola Mundo! Esto es un mensaje logueado desde un módulo escrito en Typescript importado usando Parcel.js');

var casa = function(hola) {
    var hola = hola;
    return hola;
}